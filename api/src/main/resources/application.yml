
server:
  servlet:
    context-path: /api

spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql=true:

api:
  security:
    token:
      secret: jwt-secret
#logging:
#  level:
#    org.springframework.security: DEBUG # Define o n�vel de log para a categoria de seguran�a do Spring
